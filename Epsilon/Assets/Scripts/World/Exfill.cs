using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Exfill : MonoBehaviour
{
    //TODO: Create a class which creates a point in the map for the player to go to when they have their intelligence
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
