using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Radio : MonoBehaviour
{
    //TODO: Create a radio class, this is primarily cosmetic and should just detect if the commander is holding it but could have more functionality if the player picks it up
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
