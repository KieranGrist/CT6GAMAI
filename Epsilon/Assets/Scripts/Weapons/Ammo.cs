using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ammo : MonoBehaviour
{
    //TODO: Create a class which holds the ammount of ammo a weapon has. Ammo is universal
    //TODO: Create functionality that allows entities to pick up a,mmo and store it in their inventory. If they die they drop their current ammo on the floor.
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
