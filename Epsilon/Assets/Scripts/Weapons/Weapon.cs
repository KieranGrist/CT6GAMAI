using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Weapon : MonoBehaviour
{
    //TODO: Create a class which can fire weapons based on input into the functions and hold data that allows the weapon to chage
    //TODO: Create functionality that allows entities to pick up weapons and hold them in their inventory. 
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
